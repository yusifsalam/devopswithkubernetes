apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: project
  template:
    metadata:
      labels:
        app: project
    spec:
      volumes:
        - name: img-file
          persistentVolumeClaim:
              claimName: project-claim
      containers:
        - name: project
          image: salamy1/dwk-project
          volumeMounts:
            - name: img-file
              mountPath: /usr/src/app/files